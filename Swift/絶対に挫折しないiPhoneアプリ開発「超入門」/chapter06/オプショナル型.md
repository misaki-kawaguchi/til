# オプショナル型
会員登録におけるオプション項目（書いても書かなくてもよい項目）と同じ意味を持つ。

# nil
「何もない」ことを表す特別な値。
```
var 変数名：型名? = nil
```
nilを取り得る整数型
```
var age:Int? = nil
```

```
var age:Int? = 25
print(age! + 1)
```

## 「!」を省略する書き方
```
var age:Int! = 25
print(age + 1)
```
